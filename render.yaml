services:
  - type: web
    name: studygirl
    env: python
    buildCommand: "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate && python manage.py seed_demo"
    startCommand: "daphne study_girl.asgi:application --bind 0.0.0.0 --port $PORT"
    envVars:
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: DATABASE_URL
        fromDatabase:
          name: studygirl-db
          property: connectionString
databases:
  - name: studygirl-db
    databaseName: study_girl
    user: study_girl
